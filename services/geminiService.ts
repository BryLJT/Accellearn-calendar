// This service has been deprecated and disabled as per user request.
// The app no longer uses Google GenAI.

export const parseEventWithAI = async () => {
  console.warn("AI parsing is disabled.");
  return null;
};
